# Never commit real keys. Use keys from the SAME Stripe sandbox.
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME
# Must exactly match the public website origin (no path).
BASE_URL=http://localhost:4242
PORT=4242
# Optional for the legacy webhook/subscription demo routes:
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME
PLATFORM_SUBSCRIPTION_PRICE_ID=price_REPLACE_ME
